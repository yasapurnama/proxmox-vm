# Terraform state files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version control
# as they are data points which are potentially sensitive and subject to change.
*.tfvars

# Ignore override files as they are usually used to override resources locally.
# These should not be part of version control as they are local to a machine and
# not applicable to a team/project environment.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to gitignore to avoid accidental application of configurations.
*.tfplan

# Ignore any lock files generated by terraform providers
.terraform.lock.hcl

# Ignore hidden directories
.terraform/
.terraformrc
terraform.rc

# Ignore local script logs and output files
*.log
*.out

# Ignore sensitive information files
secrets.yaml
secrets.json

# Other common directories/files to ignore
.DS_Store